{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs for the bundled connect page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
